spring:
  cloud:
    config:
      server:
        git:
          uri: ${REPO_URL}
          username: ${USERNAME}
          password: ${PASSWORD}

security:
  user:
    name: ${CONFIG_SERVER_USER}
    password: ${CONFIG_SERVER_PASSWORD}